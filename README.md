Just trying to learn some data analysis skills such as SQL, numpy, sklearn, pandas.

The idea is that maybe by collecting data from the polymarket API (which is taking me a lot longer than I expected),
I could do some stats shennanigans (Im currently taking a linear regression class so probably trying a multivariable
logistic regression where the dependant variable is whether the trade succeeded or not) and find some sort of 
relationship between winning trades and non-dynamic attributes of that trades (ie. details of the trade that you 
have access only at the time the trade is performed). 

Maybe try some machine learning at the same time since it seems to be a pretty powerful tool.
 

Old Db had ~1m trades 
Outsourced new db from huggingface https://huggingface.co/datasets/SII-WANGZJ/Polymarket_data


